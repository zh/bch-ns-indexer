PORT=3100

# -- Scanner config (bch-consumer.js) --
# Scanner makes direct HTTP calls to the fullstack.cash REST API.
# BCH_REST_URL must include the version path with trailing slash.
BCH_REST_URL=https://api.fullstack.cash/v5/
SCAN_DELAY_MS=15000

# -- Scanner with local psf-bch-api --
# BCH_REST_URL=http://192.168.0.10:5942/v6/
# SCAN_DELAY_MS=0

START_BLOCK=850000
DB_PATH=./data/bchns.sqlite

# -- Wallet config (for CLI scripts: create/update/delete/transfer) --
# Wallet scripts can use either rest-api or consumer-api.
WALLET_INTERFACE=consumer-api
WALLET_REST_URL=https://free-bch.fullstack.cash

# -- Wallet with local node --
# WALLET_INTERFACE=rest-api
# WALLET_REST_URL=http://192.168.0.3:5942/v6/

# -- Admin auth for GET /api/names --
# When both are set, the /api/names endpoint requires HTTP Basic Auth.
# When unset, the endpoint remains open (no auth required).
# ADMIN_USER=admin
# ADMIN_PASS=changeme
