PORT=3100
# rest-api is required — the scanner uses Blockchain.getBlockHash() and
# Blockchain.getBlockchainInfo() which are only available through the REST interface.
BCH_INTERFACE=rest-api
BCH_REST_URL=https://api.fullstack.cash/v5/
START_BLOCK=850000
# Local: ./data/bchns.sqlite
# Railway: /data/bchns.sqlite (persistent volume)
DB_PATH=./data/bchns.sqlite
